[pytest]
; Adds this directory to sys.path so `import main`, `import auditor`, etc.
; work regardless of which directory `pytest` is invoked from.
pythonpath = .
testpaths = .
python_files = test_*.py
