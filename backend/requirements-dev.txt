# Test-only dependencies, on top of the runtime requirements. Install with:
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.3.0
pytest-cov>=5.0.0
# fastapi.testclient.TestClient is httpx-based (not requests-based) as of
# recent FastAPI/Starlette versions. Starlette now prefers `httpx2` (plain
# `httpx` still works, but emits a StarletteDeprecationWarning on every
# TestClient use) - installing httpx2 silences that warning.
httpx>=0.27.0
httpx2>=2.0.0
